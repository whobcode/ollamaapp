# Copy to `.dev.vars` for local dev; set in prod with `wrangler secret put OLLAMA_API_KEY`.
# Ollama Cloud API key (https://ollama.com). MODEL and OLLAMA_HOST are non-secret vars in wrangler.toml.
OLLAMA_API_KEY=
